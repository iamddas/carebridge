spring:
  datasource:
    url: jdbc:mysql://localhost:3306/community_help
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

app:
  jwt:
    secret: carebridgesecretkeyforjwttoken12345678901234567890
    expiration: 86400000  # 24 hours in milliseconds

logging:
  level:
    root: INFO
    com.iamddas.communityhelp: DEBUG